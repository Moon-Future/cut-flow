export type VideoTemplate = {
  id: string;
  name: string;
  backgroundColor: string;
  surfaceColor: string;
  foregroundColor: string;
  mutedColor: string;
  accentColor: string;
  captionActiveColor: string;
  brandLabel: string;
  overlay: string;
  borderRadius: number;
};
